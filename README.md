# agentic-mcps Homebrew tap

Install the Go MCP server and its companion binaries:

```sh
brew install agentic-mcps/tap/agentic-go
```

The formula follows the signed release archives from [`agentic-mcps/go`](https://github.com/agentic-mcps/go) and supports Darwin/Linux on amd64 and arm64.
